// TODO
#![allow(unused_imports)]

use permutohedron::factorial;
use proconio::{fastout, input, marker::*};

fn run(n: usize) {}

#[fastout]
fn main() {
    input! {
        n: usize,
    };
    run(n);
}
