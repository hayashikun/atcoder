[package]
name = "abc173"
version = "0.1.0"
authors = ["Ryosuke Hayashi <hayashi@hayashikun.com>"]
edition = "2018"

# dependencies added to new project
[dependencies]
proconio = "0.4.1"

[profile.release]
lto = true
panic = 'abort'
